# Define a method meal_choice that returns the meal_choice that was
# passed into it and defaults to meat.

def meal_choice(meal = "meat") #method that looks for argument for meal and has a default of meat
  meal
end
